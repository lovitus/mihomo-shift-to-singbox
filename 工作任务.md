这是一份完整的clashmeta/mihomo的启动配置文件，现在需要将其转换为singbox的配置文件
- 里面proxy的主机和密码我隐藏了, 但是不影响你理解, 甚至启动配置.
- 里面存在节点订阅(proxy provider), 如果singbox不支持, 请使用其他方式实现,或者不用了, 因为我很少用外部节点.
- 里面存在大量的规则订阅, 一部分是公网上的,你可以全部拉取,查看内容, 如果singbox不支持这类格式, 你需要找到几乎完美的替代方案.
- 里面存在一些本地托管的规则订阅, 你在我本机发起命令,是可以直接获取到的, 但是如果singbox不支持, 你需要找到几乎完美的替代方案. 因为有自定义的, 所以如果找不到, 你可以写一个go小程序, 透明转换规则, 比如 127.0.0.1:8081/convertrulefrommihomo2singbox/xxx.yml
- 里面存在大量的dialerproxy, 不要搞错了.
处理完了给我汇报所有细节, 哪些节点处理了, 哪些有问题, 哪些规则处理了, 哪些有问题.等等
确保这个文件能几乎等效进行转换.
你也可以本地运行singbox进行测试, 除了节点不可用,其他应该都能正常加载.
运行时候使用直连模式, 避免影响我目前在用的mihomo